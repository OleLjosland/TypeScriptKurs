// GENERICS OG KLASSER

// Lag genericFunction og interface slik at det blir riktig utskrift

/*
interface GenericInterface {
    text: string
}

function genericFunction<T extends GenericInterface>(argument: T): string {
    return argument.text;
}

console.log(genericFunction({ text: 'text' })); // Skal skrive ut 'text'

*/