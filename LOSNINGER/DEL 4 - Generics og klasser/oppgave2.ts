// GENERICS OG KLASSER

// Lag klassen (og nødvendig interface) for Klasse 

/*
interface TextObject {
    text: string;
}

class Klasse {
    private num: number;
    private textObject: TextObject;

    constructor(num: number, textObject: TextObject) {
        this.num = num;
        this.textObject = textObject;
    }

    getNumberString(): string {
        return this.textObject.text + this.num;
    }
}

var klasseObjekt = new Klasse(20, { text: 'Tallet er: ' });
console.log(klasseObjekt.getNumberString()); // Skal skrive ut "Tallet er: 20"

*/
